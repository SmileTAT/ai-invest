# CLAUDE.md — ai-invest 工作指南

本仓库是我的**个人价值投资成长仓库**：借助 AI、自主阅读、实战操作，
持续记录并提升自己的价值投资能力。它不是产品，不是自动交易系统，
产出只给我自己看（仓库为私有）。

> 此前的「AI 投资研究助手」产品蓝图已归档至 `docs/archive/PRD-product-vision.md`，
> 未来方法论成熟后可能重启，当前不投入。

## 三条输入线

1. **AI 辅助** — Claude 按下方角色参与研究、拷问与复盘
2. **自主阅读** — 经典书籍与高质量文章，笔记进 `reading/`
3. **实战** — 真实持仓（A 股 + 美股），每笔决策留痕、事后复盘

## 目录结构

```
ai-invest/
├── CLAUDE.md           # 本文件
├── principles.md       # 投资原则清单：买入检查清单 + 教训（持续迭代，复利所在）
├── decisions/          # 决策记录（核心）：每笔买/卖/主动不动一份文件
├── companies/          # 个股研究档案：每个标的一份长期维护的文件
├── reading/            # 读书/阅读笔记：每本书（或重要文章）一份
├── reviews/            # 季度复盘 + 年度大复盘
├── tools/              # 单文件脚本（估值计算、数据整理、报告生成）
├── reports/            # tools/ 生成的静态 HTML（提交入库，本地浏览器打开）
└── docs/archive/       # 归档文档（原产品 PRD 等）
```

命名：`decisions/YYYY-MM-DD-<代码>-<动作>.md`（如 `2026-07-15-600519-buy.md`）、
`companies/<代码>-<名称>.md`、`reading/<书名拼音或英文>.md`、`reviews/YYYY-QN.md`。
每类目录下的 `_template.md` 是模板，新文件从它复制。

## AI 的四个角色

| 角色 | 何时触发 | 用什么 |
|---|---|---|
| **魔鬼代言人（核心）** | 每笔买入/卖出决策前，**必须**先过一轮拷问 | `grilling` 技能：拷问论点、逼出反方观点与证伪条件，直到决策记录写完整 |
| 研究助手 | 研究新标的 / 更新个股档案 | `research` 技能：调研公司与行业，结论必须带来源与日期，写入 `companies/` |
| 陪读导师 | 读书遇到硬概念（估值、会计、护城河） | `teach` 技能：讲透概念并检验我的理解，产出进 `reading/` |
| 复盘教练 | 每季度 / 每年 | 对照 `decisions/` 与实际结果，逐条判定论点状态，把教训提炼进 `principles.md` |

**AI 红线**：不替我做买卖决定；不输出"必涨/保证收益"类判断；
帮我找的永远优先是**反面证据**，而不是支持我已有观点的证据。

## 三个核心工作流

### ① 决策工作流（事件驱动）

研究（更新 `companies/` 档案）→ 用 grilling 拷问论点 → 从 `_template.md`
写决策记录（反方论点与证伪条件不许留空）→ 对照 `principles.md` 检查清单 → 执行。
**顺序不可颠倒：先写记录，后下单。**

### ② 事件更新（财报 / 重大新闻 / 证伪条件被触发）

更新对应 `companies/` 档案的跟踪日志；若证伪条件被触发，
立即在原决策记录的复盘区写结论，该卖则走一遍决策工作流。

### ③ 季度复盘

复制 `reviews/_template.md` → 逐条检查未平仓决策的论点是否仍成立 →
更新 `principles.md`（新教训、清单修订）→ 跑 `tools/build_dashboard.py` 刷新总览页。
年度复盘额外回答：今年最大的错误是什么？它值多少钱？换来了哪条原则？

## 工程约定

- 记录一律中文 Markdown，带 YAML frontmatter（模板已定义字段，别加没必要的字段）。
- `tools/` 只放 **Python 3 单文件脚本**，标准库优先；脚本中金额/价格用 `Decimal`，禁止 `float`。
- HTML 呈现：脚本输出到 `reports/`，自包含单文件（内联 CSS，无外部依赖），本地打开或会话内 Artifact 预览。仓库私有，**不用 GitHub Pages**。
- 数据引用写明来源与截止日期；AI 给的数字必须可溯源，不确定就标注不确定。
- 本仓库含真实持仓，属敏感内容：不外发、不贴到公开渠道；若要分享，先人工脱敏另建发布物。
- 提交信息英文祈使句（`Add decision record for 600519`），一次提交一个逻辑变更。

## 常用命令

```bash
python3 tools/build_dashboard.py   # 重新生成 reports/index.html 总览页
```
